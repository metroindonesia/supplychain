services:
  redis:
    image: redis:latest
    container_name: redis
    hostname: redis
    restart: no #unless-stopped
    volumes:
      - ../.data/redis:/data
    ports:
      - "6379:6379"
    networks:
      - docker1

networks:
  docker1:
    name: docker1
    external: true
